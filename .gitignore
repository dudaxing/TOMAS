# Extracted text of the copyrighted paper — do not distribute
paper_text.txt

# Python caches / build
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments (the WSL venv lives in ~/tomas-venv, not here)
.venv/
venv/

# OS / editor junk
.DS_Store
Thumbs.db

# Local Claude Code harness config (not part of the reproduction)
.claude/

# Verbose run logs and intermediate optimizer checkpoints (regenerable)
results/logs/
**/net.pt

# Feishu/Lark CLI install artifacts (not part of the TOMAS reproduction)
.agents/
skills-lock.json

# One-off PR helper from an earlier session (not part of the reproduction)
tools/create_pr.py

# Rendered pages from the copyrighted paper PDF (local analysis aid only)
results/paper_pages/
